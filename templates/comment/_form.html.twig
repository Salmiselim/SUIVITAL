{{ form_start(form, {'action': path('app_comment_new', {'postId': post.id}), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}