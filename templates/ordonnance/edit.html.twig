{% extends 'base.html.twig' %}

{% block title %}Edit Ordonnance{% endblock %}
{% block content %} 
    <div class="container mt-4">
        <h1 class="mb-4">Edit Ordonnance</h1>

        <div class="card p-4 shadow-sm">
            {{ include('ordonnance/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="mt-3">
            <a href="{{ path('app_ordonnance_index') }}" class="btn btn-secondary">Back to list</a>
        </div>

        {{ include('ordonnance/_delete_form.html.twig') }}
    </div>
{% endblock %}