<nav class="navbar navbar-top navbar-expand bg-white border-bottom shadow-sm">
    <div class="container-fluid">
        <!-- Left Navigation -->
        <div class="d-flex align-items-center">
            <!-- Sidebar Toggle Button -->
            <button class="btn btn-primary me-3" type="button" data-bs-toggle="offcanvas" data-bs-target="#sidebarMenu">
                ☰
            </button>

            <!-- Brand Name -->
            <a class="navbar-brand fw-bold text-success" href="#">
                <i class="ni ni-tv-2"></i> Suivital
            </a>

            <!-- Menu Links (Hidden on Small Screens) -->
            <ul class="navbar-nav d-none d-md-flex ms-4">
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="ni ni-single-copy-04"></i> Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="ni ni-satisfied"></i> À propos
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="ni ni-settings"></i> Services
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="ni ni-settings"></i> Contactez-nous
                    </a>
                </li>
            </ul>
        </div>

        <!-- Right Navigation (User Account) -->
        <ul class="navbar-nav ms-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="ni ni-circle-08"></i> {{ app.user.username }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-end">
                        <a class="dropdown-item" href="#">
                            <i class="ni ni-single-02"></i> My Profile
                        </a>
                        <a class="dropdown-item text-danger" href="#">
                            <i class="ni ni-user-run"></i> Logout
                        </a>
                    </div>
                </li>
            {% else %}
                <!-- Circular Login Button -->
                <li class="nav-item">
                    <button class="btn btn-outline-dark rounded-circle d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                        <i class="ni ni-key-25">                <img src="{{ asset('assets/img/login.png') }}" class="navbar-brand-img" alt="Logo" style="width: 20px; height: 20px;">
</i>
                    </button>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
