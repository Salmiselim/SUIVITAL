<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ordonnance PDF</title>
    <style>
        /* General Styles */
        body { 
            font-family: 'Arial', sans-serif;
            font-size: 14px;
            line-height: 1.6;
            color: #333;
            background: #fff;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 90%;
            margin: auto;
            padding: 20px;
            border: 1px solid #ddd;
            box-shadow: 2px 2px 10px rgba(0,0,0,0.1);
            background: #fff;
        }

        /* Header */
        .header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 2px solid #3CB371;
        }
        .header img {
            max-width: 150px;
            display: block;
            margin: 0 auto 10px;
        }
        .header h1 {
            color: #3CB371;
            font-size: 24px;
            margin: 5px 0;
        }
        .header p {
            margin: 5px 0;
            font-size: 16px;
        }

        /* Prescription Details */
        .details {
            margin-top: 20px;
        }
        .details p {
            font-size: 16px;
            margin: 5px 0;
        }

        /* Table */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            border-radius: 10px;
            overflow: hidden;
        }
        table, th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        th {
            background-color: #3CB371;
            color: #fff;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Footer */
        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 12px;
            color: #555;
            border-top: 1px solid #ddd;
            padding-top: 10px;
        }

        /* Signature */
        .signature {
            margin-top: 40px;
            text-align: right;
        }
        .signature img {
            max-width: 200px;
            display: block;
            margin-top: 10px;
        }
    </style>
</head>
<body>

    <div class="container">

        <!-- Header with Logo -->
        <div class="header">
<img src="{{ asset('assets/SUIVITAL.png') }}" alt="l Logo">
            <h1>Ordonnance #{{ ordonnance.id }}</h1>
            <p>Date: <strong>{{ ordonnance.datePrescription|date('d/m/Y') }}</strong></p>
        </div>

        <!-- Patient & Doctor Info -->
        <div class="details">
            <p><strong>Patient:</strong> {{ ordonnance.patient.getName() }}</p>
            <p><strong>Doctor:</strong> {{ ordonnance.doctor.getName() }}</p>
            <p><strong>Description:</strong> {{ ordonnance.description }}</p>
        </div>

        <!-- Medications Table -->
        <h2>Prescribed Medications</h2>
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Dosage</th>
                    <th>Duration</th>
                    <th>Frequency</th>
                </tr>
            </thead>
            <tbody>
                {% for medicament in ordonnance.medicaments %}
                    <tr>
                        <td>{{ medicament.name }}</td>
                        <td>{{ medicament.dosage }}</td>
                        <td>{{ medicament.duration }} days</td>
                        <td>{{ medicament.frequency }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" style="text-align: center;">No medications prescribed.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <!-- Signature -->
        <div class="signature">
            <p><strong>Doctor's Signature:</strong></p>
            <img src="{{ asset('doctor_signature.png') }}" alt="Doctor Signature">
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Generated by Suivital | Confidential</p>
        </div>

    </div>

</body>
</html>
