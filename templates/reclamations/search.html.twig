{# templates/reclamations/search.html.twig #}
{% extends 'alabase.html.twig' %}

{% block body %}
    <h1>Résultats de recherche</h1>

    <form method="get" action="{{ path('app_reclamations_search') }}">
        <input type="text" name="searchTerm" value="{{ searchTerm }}" placeholder="Rechercher...">
        <button type="submit">Rechercher</button>
    </form>

    <h2>Reclamations</h2>
    <ul>
        {% for reclamation in reclamations %}
            <li>{{ reclamation.objet }} - {{ reclamation.commentaire }}</li>
        {% endfor %}
    </ul>

    {{ knp_pagination_render(reclamations) }}
{% endblock %}
